<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
@if (!Context.Request.Path.StartsWithSegments("/Admin"))
{
    <script src="~/js/auth.js" asp-append-version="true"></script>
}
<script>
(function() {
    'use strict';
    
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
    
    function init() {
        console.log("✅ DOM Ready. Initializing features...");
        initNavbarAndScroll();
        initMobileSearch();
    }
    
    function initNavbarAndScroll() {
        const navbar = document.getElementById('mainNavbar');
        const scrollBtn = document.getElementById('scrollTopBtn');
        const body = document.body;

        if (!navbar) {
            console.log("⚠️ Navbar not found. Aborting scroll effects.");
            return;
        }

        console.log("✅ Navbar found. Setting up scroll listener...");
        console.log("📍 Body classes:", body.className);
        console.log("📍 Is home page:", body.classList.contains('home-page'));

        const isHomePage = body.classList.contains('home-page');
        
        // Force set initial state bằng inline style
        if (isHomePage && window.innerWidth >= 992) {
            navbar.style.background = 'transparent';
            navbar.style.boxShadow = 'none';
            console.log("🎨 Initial state: Transparent navbar (forced via JS)");
        }

        let ticking = false;

        function handleScroll() {
            const scrollPosition = window.pageYOffset || document.documentElement.scrollTop;
            
            if (!ticking) {
                window.requestAnimationFrame(() => {
                    const isDesktop = window.innerWidth >= 992;
                    
                    // Chỉ áp dụng hiệu ứng transparent cho trang chủ ở desktop
                    if (isHomePage && isDesktop) {
                        if (scrollPosition > 50) {
                            // Set gradient xanh bằng inline style
                            navbar.style.background = 'linear-gradient(135deg, #012c42 0%, #015487 50%, #0477eb 100%)';
                            navbar.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.3)';
                            navbar.classList.add('navbar-scrolled');
                            console.log("🎨 Navbar scrolled - Gradient applied (scroll: " + scrollPosition + "px)");
                        } else {
                            // Set transparent bằng inline style
                            navbar.style.background = 'transparent';
                            navbar.style.boxShadow = 'none';
                            navbar.classList.remove('navbar-scrolled');
                            console.log("🎨 Navbar at top - Transparent (scroll: " + scrollPosition + "px)");
                        }
                    } else if (isHomePage && !isDesktop) {
                        // Mobile - luôn có màu
                        navbar.style.background = 'linear-gradient(135deg, #012c42 0%, #015487 50%, #0477eb 100%)';
                        navbar.style.boxShadow = '0 2px 10px rgba(0, 0, 0, 0.3)';
                        navbar.classList.remove('navbar-scrolled');
                        console.log("📱 Mobile - Navbar always colored");
                    }

                    // Hiển thị/ẩn nút scroll to top
                    if (scrollBtn) {
                        if (scrollPosition > 300) {
                            scrollBtn.classList.add('show');
                            console.log("⬆️ Scroll button shown (scroll: " + scrollPosition + "px)");
                        } else {
                            scrollBtn.classList.remove('show');
                            console.log("⬇️ Scroll button hidden (scroll: " + scrollPosition + "px)");
                        }
                    }
                    
                    ticking = false;
                });
                ticking = true;
            }
        }

        // Lắng nghe scroll trên window
        window.addEventListener('scroll', handleScroll, { passive: true });
        console.log("✅ Scroll listener attached to window.");

        // Xử lý resize
        window.addEventListener('resize', () => {
            console.log("📐 Window resized. Width:", window.innerWidth);
            if (window.innerWidth < 992 && isHomePage) {
                navbar.style.background = 'linear-gradient(135deg, #012c42 0%, #015487 50%, #0477eb 100%)';
                navbar.style.boxShadow = '0 2px 10px rgba(0, 0, 0, 0.3)';
                navbar.classList.remove('navbar-scrolled');
            } else if (window.innerWidth >= 992 && isHomePage && window.pageYOffset === 0) {
                navbar.style.background = 'transparent';
                navbar.style.boxShadow = 'none';
            }
            handleScroll();
        });

        // Chạy lần đầu để set trạng thái ban đầu
        handleScroll();
        
        // Xử lý click nút scroll to top
        if (scrollBtn) {
            scrollBtn.addEventListener('click', (e) => {
                e.preventDefault();
                window.scrollTo({ 
                    top: 0, 
                    behavior: 'smooth' 
                });
                console.log("🚀 Scrolling to top...");
            });
            console.log("✅ Scroll to top button initialized.");
        } else {
            console.log("⚠️ Scroll to top button not found.");
        }
    }

    function initMobileSearch() {
        const mobileSearchBtn = document.getElementById('mobileSearchBtn');
        const mobileSearchOverlay = document.getElementById('mobileSearchOverlay');
        const mobileSearchClose = document.getElementById('mobileSearchClose');
        const mobileSearchInput = document.getElementById('mobileSearchInput');
        
        if (!mobileSearchBtn || !mobileSearchOverlay) {
            return;
        }
        
        function closeMobileSearch() {
            mobileSearchOverlay.classList.remove('active');
            document.body.classList.remove('search-overlay-active');
        }
        
        mobileSearchBtn.addEventListener('click', function(e) {
            e.preventDefault();
            mobileSearchOverlay.classList.add('active');
            document.body.classList.add('search-overlay-active');
            setTimeout(() => {
                if (mobileSearchInput) mobileSearchInput.focus();
            }, 300);
        });
        
        if (mobileSearchClose) {
            mobileSearchClose.addEventListener('click', closeMobileSearch);
        }
        
        mobileSearchOverlay.addEventListener('click', function(e) {
            if (e.target === mobileSearchOverlay) {
                closeMobileSearch();
            }
        });
        
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape' && mobileSearchOverlay.classList.contains('active')) {
                closeMobileSearch();
            }
        });
        
        const suggestionTags = document.querySelectorAll('.suggestion-tag');
        suggestionTags.forEach(tag => {
            tag.addEventListener('click', function() {
                if (mobileSearchInput) {
                    mobileSearchInput.value = this.textContent;
                    mobileSearchInput.focus();
                }
            });
        });
    }
})();
</script>