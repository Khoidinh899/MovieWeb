# Bỏ qua file môi trường
.env # Environment variables
.env.local  # Local environment variables
.env.production # Production secrets - KHÔNG commit!
firebase-key.json # Firebase service account key - KHÔNG commit!
# Bỏ qua thư mục .vs của Visual Studio
/.vs

# Bỏ qua file setting cá nhân của project
*.csproj.user

# Bỏ qua file build
/bin
/obj

# Bỏ qua file cấu hình deploy
Properties/PublishProfiles/

# Bỏ qua file .agent
*.agent